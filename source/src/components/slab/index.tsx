/**
 * The site's icon set. Every component imports icons from here, not from the
 * library directly, so the whole site can swap icon sets by changing this one
 * file. Phosphor (MIT) - https://phosphoricons.com.
 */
export * from '@phosphor-icons/react'
